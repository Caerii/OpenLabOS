<?xml version="1.0" encoding="utf-8"?>
<!--
  Migration helper manifest.
  Mirrors the old LabOS app's AdminReceiver so we can clear device owner.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="LabOS Migration"
        android:allowBackup="false">

        <!-- Must match the old AdminReceiver component name exactly -->
        <receiver
            android:name=".AdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Activity that clears device owner on launch -->
        <activity
            android:name=".ClearOwnerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
