# OpenLabOS Devtools

Small repository-local tools for producing operator, architecture, and release artifacts.

```powershell
pnpm --filter @openlabos/devtools slides
pnpm --filter @openlabos/devtools slides:overview
```

Slide outputs are written under `devtools/slides/out/`.
